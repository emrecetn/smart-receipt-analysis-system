.env
__pycache__/
*.pyc
gecici_*.jpg
temp_*
.pytest_cache/

# Training dataset photos are not pushed to GitHub (large + may contain
# personal data); the original source is the Kaggle link in the report's
# SOURCES/APPENDIX section. Small config files like data.yaml and
# classes.txt still stay in the repo.
ml/dataset/images/
ml/dataset/labels/
